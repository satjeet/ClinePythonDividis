/**
 * UserLog.ts
 * Lógica y props para el log de usuario: últimas declaraciones y misiones activas.
 * Preparado para recibir listas de declaraciones y misiones como props en el futuro.
 */

import { defineComponent } from 'vue'

export default defineComponent({
  name: 'UserLog'
  // Props y lógica se agregarán en la siguiente iteración.
})
